declared-services:
  dialog-service-beta:
    label: dialog
    plan: beta
applications:
- services:
  - dialog-service-beta
  name: dialog-nodejs
  command: node app.js
  path: .
  memory: 512M
  env:
    NODE_ENV: production
